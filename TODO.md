# TODO: Resolve Python Version Mismatch in Pipfile

- [x] Update `python_full_version` in Pipfile from "3.8.13" to "3.8.10"
- [ ] Run `pipenv run pytest` to verify the warning is resolved and tests still pass
- [ ] If necessary, run `pipenv install` to reinstall dependencies for compatibility
